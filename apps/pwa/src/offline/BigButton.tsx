export default function BigButton(props: any) {
  return (
    <button
      {...props}
      style={{
        width: '100%',
        padding: 16,
        fontSize: 18,
        marginTop: 12
      }}
    />
  );
}
